# Blob SDK – Exam Traps

❗ BlobServiceClient ≠ BlobClient
→ different scopes

❗ Metadata is NOT automatically indexed
→ just key-value storage

❗ SDK is preferred over REST
→ unless specific requirement

❗ Container must exist before uploading blobs
